Peeps by other makers:
<ul>
  <% @peeps.each do |peep| %>
    <li><%= peep.message %> by <%= peep.posted_by_username %> aka <%= peep.posted_by_name %> <%= timeago(peep.created_at) %></li>
  <% end %>


</ul>
